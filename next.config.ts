import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["linnea-inexplosive-myrtis.ngrok-free.dev"],
  reactCompiler: true,
  reactStrictMode: true,
};

export default nextConfig;
